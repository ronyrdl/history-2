# history-2
This program is a simple inventory management system that allows the user to add products, view the inventory, 
and calculate statistics such as total value and total quantity. It uses functions to organize the code, validates user input to avoid errors, 
and stores products in a list of dictionaries. The program runs through a menu system until the user chooses to exit.
